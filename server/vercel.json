{
    "installCommand": "cd .. && pnpm install --frozen-lockfile",
    "version": 2,
    "routes": [
        {
            "src": "/.*",
            "dest": "/server.js"
        }
    ],
    "functions": {
        "server.js": {
            "maxDuration": 60
        }
    }
}